#%%
# Packages
import datetime as dt # current time: dt.datetime.now()
import csv
#%%
# Open the File
file_to_load = 'Resources/elections_results.csv'
with open(file_to_load) as election_data:
    # Get list of all candidate names
    # Create votecount variable for each variable
    # Total votes for each name and store in related votecount
    # Sum all votes to get total vote count
    # Divide each candidate's votes by total count to get percentage



